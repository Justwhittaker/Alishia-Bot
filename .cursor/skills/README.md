# Project skills

Slash-command skills for this workspace. Commit these so they travel with the repo.

| Skill | Command | Purpose |
|-------|---------|---------|
| `shopify_csv` | `/shopify_csv` | Convert scraped product CSVs → Shopify import CSVs (**always unlisted**) |
